<div class="mt-4">
    <div class="row">
        <div class="col-lag-9">
            <h2>{{post.name}}</h2>
            <p>{{post.description}}</p>
            <img src="{{post.thumbnail}}" width="1110">
        </div>
    </div>
</div>